{
    "forEach": "item-psp22-contracts",
    "filename": "packages/contract-{{kebabCase name}}/src/lib.rs"
}
---
#![cfg_attr(not(feature = "std"), no_std, no_main)]

#[ink::contract]
pub mod {{snakeCase name}} {
    pub use ink::prelude::string::String;

    /** Error type for PSP-22 compatible contract for element 'air' */
    #[derive(Debug, PartialEq, Eq, Copy, Clone)]
    #[ink::scale_derive(Encode, Decode, TypeInfo)]
    pub enum Error {}

    #[ink(storage)]
    pub struct {{pascalCase name}}Contract {
        name: String,
    }

    impl Default for {{pascalCase name}}Contract {
        fn default() -> Self {
            Self {
                name: String::from("{{name}}"),
            }
        }
    }

    impl {{pascalCase name}}Contract {
        /// Creates a new PSP-22 compatible {{capitalCase name}} Element contract
        #[ink(constructor)]
        pub fn new() -> Self {
            Self::default()
        }

        #[ink(message)]
        pub fn get_name(&self)-> String {
            self.name.clone()
        }
    }

}
